using System;
using System.Collections.Generic;

using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.Content.Res;
using Android.Graphics;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

namespace AppExample.Droid
{
    class CrearImage
    {
    
        public void skdbk()
        {
            //Image pbxFoto;

            //System.Drawing.Font printFont = new System.Drawing.Font("Arial", 10, FontStyle.Bold);
            //Bitmap myBitmap1 = new Bitmap(pbxFoto.Width, pbxFoto.Height);
            //pbxFoto.DrawToBitmap(myBitmap1, new Rectangle(0, 0, pbxFoto.Width, pbxFoto.Height));
            //e.Graphics.DrawImage(myBitmap1, 0, 0);

            //int yPos = (int)(100 * myBitmap1.Height / myBitmap1.VerticalResolution);
            //yPos += 50;

            //e.Graphics.DrawString("Esto es una prueba", printFont, Brushes.Black, 10, yPos);
            //myBitmap1.Dispose();

         

            //Bitmap bitmap = BitmapFactory.DecodeResource(, Resource.Drawable.bluetooth);

             
        }
    }
}